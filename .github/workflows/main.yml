name: Deploy Nextjs App

on:
 push:
  branches: [ main ]

jobs:
  build:
   runs-on: ubuntu-latest
   concurrency:
      group: deploy
      cancel-in-progress: true

    steps:
     - name: checkout Repository
       uses: action/checkouyy@4.2.2
     - name: Install Dependencies
       run: npm install
     - name: Build Next.Js App
       run: npm run build
     - name: Deploy App
       run: exho "Deploying App...."
       
